/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com', 'd2b76rucehnxe5.cloudfront.net'], // TODO: github dummy image test를 위한 config로 기능 구현 후 제거 필요
  },
};

export default nextConfig;

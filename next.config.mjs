/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com', 'd3c0y898rmr0cs.cloudfront.net'], // TODO: github dummy image test를 위한 config로 기능 구현 후 제거 필요
  },
};

export default nextConfig;
